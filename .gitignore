node_modules
dist

# Local environment overrides — never commit secrets.
# Ignore all .env* by default; explicitly allow the safe, committed ones.
.env*
!.env.example
!.env.development
!.env.production

# Test artifacts — never commit these
playwright-report/
test-results/
coverage/
.seed-manifest.json
e2e/seed/.seed-manifest.json
